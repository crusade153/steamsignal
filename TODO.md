# 앞으로 할 일

갱신 2026-09-06 · 라이브 https://steamsignal.vercel.app

현재 상태·설계 근거·건드리면 깨지는 지점은 [HANDOFF.md](HANDOFF.md) 에 있다. 이 문서는 **할 일만** 적는다.

표기: **[사람]** 은 계정·승인·결제가 필요해 에이전트가 대신할 수 없는 일. **[코드]** 는 작업하면 끝나는 일.

---

## 0. 파이프라인 — 2026-09-06 해결됨

- [x] **[사람] 스케줄러를 cron-job.org 로 옮겼다.** GitHub 스케줄은 10분 간격에 못 쓴다 —
      시간당 9번 떠야 하는데 5시간에 2번(약 4%)이었다. 어긋난 분으로 바꾸는 것도 효과가 없었다.
      코드는 한 줄도 안 고쳤다. 구성과 확인 방법은 [HANDOFF §3-1](HANDOFF.md).
      옮긴 직후 10분 간격이 실제로 지켜지는 것을 DB 로 확인했다.
- [x] **[사람] 빠져 있던 마이그레이션을 적용했다.** 이메일 알림 테이블 3개와 `prune_subscriptions()`
      함수가 DB 에 없어서 `prune` 이 500 을 냈다. Neon 콘솔로 적용해 **테이블 11개 · 함수 5개**가 됐다.
      재발 방지는 [HANDOFF §3-2](HANDOFF.md).
- [x] **[코드] 파이프라인 감시** — `watchdog` 잡(2시간 간격). 스냅샷 지연·chart 실패·상세 실패율·
      죽은 앱 수를 보고 하나라도 걸리면 **일부러 실패한다.** cron-job.org 의 실패 알림이 메일을 보내므로
      별도 통보 설정이 필요 없다. 조건은 [docs/DATA-PIPELINE.md §7](docs/DATA-PIPELINE.md) 과 `HEALTH_LIMITS`.
- [ ] **[확인] 24시간 뒤 한 번 본다.** `captured_at` 의 서로 다른 값이 **144 근처**여야 한다.
      한참 못 미치면 크론 서비스 쪽 실행 이력을 본다.

      ```bash
      node --env-file=.env -e "import('./lib/db.mjs').then(async({getSql})=>{const s=getSql();console.table(await s\`SELECT COUNT(DISTINCT captured_at)::int AS ticks, COUNT(*)::int AS rows, pg_size_pretty(pg_database_size(current_database())) AS db FROM player_snapshots\`)})"
      ```
- [ ] **[확인] 7일째 되는 날 `prune` 의 실제 삭제를 본다.** 원시 스냅샷이 10만 행 근처에서 **멈춰야** 한다.
      계속 늘면 보관정책이 안 도는 것이고, 그러면 Neon 무료 0.5GB 를 1년 안에 넘긴다.
      (지금은 데이터가 7일 미만이라 삭제 0건이 정상이다.)

---

## 1. 색인 — 만들어 둔 페이지를 사람이 찾을 수 있게

- [ ] **[사람] Google Search Console 에 사이트맵 제출**
      `https://steamsignal.vercel.app/sitemap.xml` · URL 123개가 이미 나오고 있는데 아무도 제출하지 않았다.
      소유권 확인은 DNS 또는 HTML 태그. 태그 방식이면 `public/index.html` 과 `lib/render.mjs` 의
      `layout()` 양쪽에 넣어야 전 페이지에 붙는다.
- [ ] **[사람] Bing Webmaster Tools** — Search Console 에서 가져오기(Import)가 되므로 5분이면 끝난다
- [ ] **[확인] 1~2주 뒤 색인 상태** — `site:steamsignal.vercel.app` 로 몇 개가 잡혔는지.
      게임 페이지가 안 잡히면 내부 링크가 부족한 것이므로 허브 페이지에서 더 많이 링크한다

---

## 2. 애드센스 — **1~2주 더 돌린 뒤에** 신청

지금 신청하면 반려 가능성이 높다. 게임 페이지의 차트가 표본 몇 시간뿐이라
"데이터가 쌓이는 중"이 많이 보이고, 그 상태는 심사자에게 '가치 낮은 콘텐츠'로 읽힌다.
**동접 추이가 실제로 그려지기 시작하면 그때가 적기다.**

- [ ] **[사람] `CONTACT_EMAIL` 정하기** — 지금 `/contact` 는 GitHub 이슈만 안내한다.
      심사는 보통 이메일 연락 수단을 기대한다. 개인 메일 공개가 싫으면 별칭 주소를 쓴다.
      Vercel 환경변수에 넣으면 `/contact` 와 방침·약관의 문의 문구가 자동으로 바뀐다
- [ ] **[사람] 애드센스 신청**
- [ ] **[사람] 승인되면 환경변수 2개 등록** — `ADSENSE_PUBLISHER_ID`(`ca-pub-...`)와 `ADSENSE_SLOT_DETAIL`.
      **코드 변경은 없다.** 두 값이 들어오는 순간 광고 스크립트·슬롯·`/ads.txt` 가 한꺼번에 살아난다
- [ ] **[코드] 승인 후 in-feed 슬롯 추가** — 목록 20개마다 1개.
      `adSlot()` 이 이미 `min-height` 를 예약하므로 CLS 는 방어된다.
      넣은 뒤 **반드시 Lighthouse 로 CLS 를 재측정**할 것 — 지금은 좋은데 광고가 무너뜨리는 게 전형적이다

---

## 3. 분석

- [ ] **[사람] Vercel 대시보드 > Analytics > Enable → 그다음 `VERCEL_WEB_ANALYTICS=1` 등록**
      **순서가 중요하다.** `/_vercel/insights/script.js` 는 대시보드에서 켠 프로젝트에만 존재해서,
      변수를 먼저 넣으면 그사이에 방문자마다 404 요청이 하나씩 나간다. (실제로 한 번 그렇게 배포했다가
      live e2e 에서 잡혔다.) Hobby 무료이고 쿠키를 쓰지 않아 동의 배너가 필요 없다
- [ ] **[확인] 첫 주 Vercel Usage 확인** — 특히 **Fluid Active CPU**(월 4시간).
      수집 잡이 대부분 네트워크 대기라 낮을 것으로 보지만 **이 값만은 추정이다.**
      넘칠 것 같으면 `lib/collect.mjs` 의 `collectDetails({ batch })` 를 20에서 줄인다

---

## 4. 재방문 — 이메일 리스트는 광고보다 가치 있는 자산

위시리스트(`/watchlist`)는 이미 있다. 계정 없이 localStorage 에만 담으므로 개인정보를 수집하지 않는다.
여기서 이메일로 넘어가는 순간 성격이 달라진다 — 개인정보를 보관하게 되고,
[개인정보처리방침](https://steamsignal.vercel.app/privacy)에 수집·보관·파기 항목을 **시행 전에** 추가해야 한다.

**코드는 2026-09-06 에 다 들어갔다.** `RESEND_API_KEY` 와 `MAIL_FROM` 이 등록되는 순간 켜지고,
없는 동안은 구독 폼도 `/alerts` 도 존재하지 않는다(404). 즉 아래 [사람] 항목 하나가 남은 전부다.

- [ ] **[사람] Resend 가입 + 도메인 인증 + 환경변수 2개 등록** — `RESEND_API_KEY`, `MAIL_FROM`.
      무료 티어 월 3,000통. **도메인 인증(SPF·DKIM)을 끝내고 넣을 것** — 인증 없이 보내면
      Gmail 이 스팸함으로 보내고, 그 상태가 며칠 쌓이면 도메인 평판이 회복되지 않는다.
      넣기 전에 `MAIL_FROM` 의 주소가 실제로 받을 수 있는 주소인지도 확인한다(수신 거부 문의가 여기로 온다)
- [ ] **[확인] 켠 뒤 첫 발송** — 본인 주소로 신청 → 확인 메일 → `/alerts/confirm` 버튼 → 가격 알림.
      `mail_deliveries` 에 `status='sent'` 가 남는지 DB 로 확인하는 게 가장 정확하다:

      ```bash
      node --env-file=.env -e "import('./lib/db.mjs').then(async({getSql})=>{const s=getSql();console.log(await s\`SELECT kind, status, COUNT(*)::int FROM mail_deliveries GROUP BY 1,2\`)})"
      ```
- [x] **[코드] 구독 테이블** — `subscribers` / `price_alerts` / `mail_deliveries` 3개.
      더블 옵트인, 주소별 영구 수신거부 토큰, IP 미저장
- [x] **[코드] 가격 하락 감지 크론** — `alerts` 잡(30분 간격). `notified_price` 워터마크로
      같은 할인에 두 번 보내지 않고, 한 사람의 여러 건은 한 통으로 묶는다
- [x] **[코드] 개인정보처리방침 갱신** — 수집 항목·목적·보관 기간(30/30/90일)·파기·Resend 위탁·해지 방법.
      메일이 꺼져 있으면 이 항목 자체가 방침에서 사라진다(절 번호도 자동으로 당겨진다)
- [x] **[코드] 주간 리포트 뉴스레터** — `newsletter` 잡(월요일 09:23 KST). 실을 내용이 없으면 보내지 않는다

---

## 5. 콘텐츠 — URL 을 더 늘린다

사이트맵 URL 이 123개 → **160개**가 됐다(2026-09-06). 리뷰 추이 페이지는 아직 0개다 —
`review_daily` 가 3일 이상 쌓인 게임만 싣기 때문이고, 그건 §0 이 풀려야 쌓인다.

- [x] **[코드] `/game/<appid>/reviews`** — 누적 긍정률과 최근 신규 리뷰 긍정률의 차이를 보여 준다.
      차분 표본이 2구간 미만이면 `noindex`(404 가 아니다 — 나중에 쌓여도 그 404 가 남는다).
      게임 상세에서의 링크도 볼 게 있을 때만 걸린다
- [x] **[코드] `/charts/monthly`** — 30일 평균. `/charts/weekly` 와 정의를 공유한다(`CHART_VIEWS`)
- [x] **[코드] `/deals/all-time-low`** — 가격 변동을 **두 번 이상** 관측한 게임만 판정한다.
      한 번만 본 가격은 자동으로 최저가가 되어 아무것도 알려 주지 못한다
- [x] **[코드] `/genre/<장르>/free`, `/genre/<장르>/discounted`** — 게임 5개 미만(`MIN_COMBO_GAMES`)이면
      만들지 않는다. 링크·페이지·사이트맵이 **같은 기준**을 쓰므로 "눌렀더니 404" 가 안 생긴다
- [x] **[코드] `/releases/<연도>`** + `/releases` 허브 — `apps.release_date` 기반. 연도는 네 자리 숫자만 받는다
- [ ] **[코드] 다음 후보** — `/charts/peak`(역대 최고 동접 순위)는 `player_daily.peak_reported` 만으로
      만들 수 있다. 다만 **§0 이 풀린 뒤에** 손댈 것. 지금은 어떤 페이지를 더 만들어도 표본이 없다

---

## 6. 어필리에이트 — 애드센스보다 RPM 이 높을 수 있다

게임 사이트는 특히 그렇다. 지금 `/deals` 와 게임 상세의 가격 블록이 그대로 전환 지점이다.

- [ ] **[사람] 프로그램 검토·가입** — Humble Partner, Fanatical, GreenManGaming 등.
      Steam 자체는 어필리에이트 프로그램이 없다
- [ ] **[코드] 링크에 `rel="sponsored"`** — 안 붙이면 검색 품질 가이드라인 위반이다
- [ ] **[코드] 어필리에이트 고지 문구** — `/terms` 와 해당 페이지에. 법적 요구이자 신뢰의 문제다

> 아래 두 [코드] 항목은 **[사람] 항목 뒤에** 한다. 지금은 어필리에이트 링크가 하나도 없어서,
> 고지 문구를 먼저 넣으면 있지도 않은 제휴 관계를 적는 것이 된다 — 그건 고지가 아니라 거짓말이다.
> 프로그램에 가입하고 링크 형식(태그 파라미터)이 정해지면 그때 둘을 함께 넣는다.

---

## 7. 운영 — 지금은 없어도 되지만 트래픽이 붙으면 필요하다

- [x] **[코드] 수집 실패 알림** — `watchdog` 잡. §0 참고. Slack·Resend 없이 GitHub 워크플로 실패를
      경보 채널로 쓴다. 나중에 Slack 을 붙이고 싶으면 `evaluateHealth()` 의 반환값을 던지는 대신
      웹훅으로 보내면 된다 — 판정과 통보가 이미 분리돼 있다
- [ ] **[코드] `/api/cron` 레이트리밋** — 지금은 시크릿만으로 막고 있다
- [ ] **[사람] Neon 백업 확인** — 무료 티어의 PITR 보존 기간을 확인하고, 부족하면
      `player_daily`(영구 보존분)만이라도 주기적으로 덤프한다. **이 테이블이 이 사이트의 자산 전부다**
- [ ] **[코드] Sentry 또는 Vercel 로그 드레인**
- [x] **[코드] `ci.yml` 의 Node 를 22 로 맞추고 `package.json` 에 `engines` 를 박았다** — 로컬·CI·Vercel 이 같은 메이저를 쓴다

---

## 8. 미검증 — 시간이 지나야 알 수 있는 것들

정직하게 적는다. 아래는 "안 될 수도 있다"가 아니라 "아직 확인 못 했다"이다.

- [ ] **롤업 겹치기** — `rollup_player_hourly(3)` 이 최근 3시간을 재집계한다. 겹쳐 돌아도
      값이 중복되지 않는지는 데이터가 며칠 쌓여야 확인된다
- [ ] **`prune_timeseries()` 의 실제 삭제** — 원시 7일 / 시간 90일. **7일째 되는 날 꼭 확인할 것.**
      안 지워지면 Neon 무료 0.5GB 를 1년 안에 넘긴다
- [ ] **`/rising` 의 기본 창(24h vs 7일)** — 쿼리 자체는 창을 좁혀(1h vs 4h) 실데이터로 검증했다.
      기본 창은 시간 롤업이 8일치 쌓여야 뜬다
- [ ] **광고 실제 게재와 CLS** — 승인 전이라 슬롯이 렌더링되지 않는다

---

## 9. 방향 — PC·모바일 동등 대응, 재방문, 커버리지 300개

운영자가 정한 지향이다. **개별 항목보다 이 절이 우선한다.**

> **PC 와 모바일 둘 다 1급으로 대응할 것. 자꾸 들어와서 확인하게 되는 사이트로 만들 것.
> 추적 게임을 300개로 늘릴 것.**

**기획·정의·측정 지표는 [docs/PRODUCT.md](docs/PRODUCT.md) 에 있다.** 여기는 할 일만 적는다.
"엔터프라이즈급"이 무엇인지, 무엇이 되면 된 것인지는 그 문서가 정의한다 — 작업 전에 읽는다.

순서는 PRODUCT.md §6 의 Phase 를 따른다. **앞 Phase 가 뒤 Phase 의 전제다.**

### Phase 1 — 깨진 것부터 (모바일)

깨진 화면 위에 기능을 쌓으면 둘 다 나빠진다. 아래는 배포본을 390x844 로 직접 재서 확인한 것이다.

- [ ] **[코드] 상단 내비가 세로로 깨진다.** 390px 에서 6개 항목이 폭을 넘겨 **한 글자씩 세로로 쌓인다.**
      2026-09-06 에 홈 내비에 `위시리스트` 를 추가하면서 생긴 회귀다.
      가로 스크롤 내비나 햄버거로 바꾼다.
- [ ] **[코드] e2e 에 "뭉개짐" 검사를 넣는다.** 지금 검사는 `scrollWidth <= innerWidth` 만 본다.
      위 회귀는 **넘치지 않고 뭉개져서** 통과해 버렸다. 내비 항목 높이가 한 줄의 2배를 넘으면 실패시킨다.
      **이 항목을 먼저 하면 위 항목의 수정이 검증된다.**
- [ ] **[코드] 좁은 화면에서 표를 카드로 전환한다.** `table { min-width: 1000px }` 이라 실폭이 820px 가 되고
      발매일·평가·메타크리틱·가격이 첫 화면 밖에 있다. `.table-scroll` 이 페이지 넘침은 막지만
      **정보가 안 보이는 것은 그대로다.** 행 하나 = 카드 하나, 지표는 2열 그리드.
      대상: `/`, `/rising`, `/deals`, `/deals/all-time-low`, `/charts/*`, `/genre/*`, `/releases/*`, 리뷰 추이.
- [ ] **[코드] 세 폭 검사를 e2e 에 고정한다** — 390 / 768 / 1440. 기준은 PRODUCT.md §2-4 의 표.
- [ ] **[확인] 실기기 확인** — 에뮬레이션과 실제 폰은 다르다. 폰트 확대 설정, 노치, 주소창 숨김이 특히 다르다.

### Phase 2 — 재방문 장치 (가장 싼 비용, 가장 큰 효과)

- [ ] **[사람] 이메일 알림을 켠다** — `RESEND_API_KEY` + `MAIL_FROM`. §4 참고.
      **재방문 장치 중 투자 대비 효과가 압도적으로 크다.** 코드도 DB 테이블도 이미 준비됐다.
- [ ] **[코드] 위시리스트에 변화(Δ)를 넣는다.** 지금은 현재값만 보여 준다.
      "어제 대비 동접 +12%", "3일 전보다 5,000원 내림". `player_daily` + `price_events` 로 계산된다.
      상태는 계속 localStorage 에만 둔다(규칙 10 — 서버가 그리면 CDN 이 남의 상태를 보여 준다).
- [ ] **[코드] 순위 변동 화살표** — 목록에 `▲3` `▼1` `NEW`. `player_daily.best_rank` 로 계산된다.
      가장 값싼 재방문 장치다.

### Phase 3 — 정보밀도

- [ ] **[코드] 목록 행에 스파크라인** — 7일 미니 차트. `lineChart()` 를 축약해 재사용한다.
      목록에서 판단이 끝나면 클릭 수가 1 에서 0 이 된다(PRODUCT.md §5).
- [ ] **[코드] 홈 첫 화면에 "오늘의 변화"** — 지금 홈은 TOP 100 순위라 어제와 거의 같다.
      순위가 가장 많이 오른 게임 / 새로 진입 / 오늘 시작된 할인을 위로 올린다.
- [ ] **[코드] 수집 상태 공개 페이지** — 마지막 수집 시각, 커버리지, 결손 구간.
      데이터를 숨기지 않는다는 신호이자, 사고가 났을 때 사용자가 스스로 확인할 수 있는 곳.

### Phase 4 — 커버리지 100개 → 300개

Steam 의 `mostplayed` 차트가 **100개만 준다**(`lib/steam.mjs` 의 `.slice(0, 100)`).
그래서 300개는 추가 소스 결정이 필요하다. 비용 계산과 방법 비교는 PRODUCT.md §4 에 있다.

- [ ] **[코드] 차트 이력 누적 방식으로 넓힌다.** 차트 잡이 "현재 100개 + 최근 N일 안에 차트에 있었던 앱"을
      함께 찍도록 바꾼다. **추가 데이터 소스가 필요 없고 위험이 없다.** 대신 300개까지 몇 달 걸린다.
      부수 효과가 크다 — 차트에서 내려간 게임의 시계열이 **더는 끊기지 않는다.**
- [ ] **[코드] `collectDetails` 의 batch 를 40 으로 올린다.** 300개면 batch 20 으로는 한 바퀴에 2.5시간이다.
      40이면 1.25시간. Steam 요청량은 80req/10분으로 알려진 한계 안이다.
      **Vercel Active CPU 를 함께 본다** — 그게 진짜 제약이다.
- [ ] **[확인] 용량 재측정.** 300개면 1년 뒤 ~135MB 로 추정된다(500MB 의 27%).
      실측이 추정을 크게 넘으면 `player_hourly` 보관을 90일 → 60일로 줄인다.

### Phase 5 — 일관성·마감

- [ ] **[코드] 디자인 토큰 정리.** `styles.css` 가 미니파이된 한 줄이라 사람이 못 고친다.
      페이지를 더 붙이기 전에 푼다. 측정: 새 페이지당 CSS 추가 5줄 이하.
- [ ] **[코드] 빈 상태 정돈.** 적재 초기라 사용자가 이걸 자주 본다.
      "언제 채워지는지"를 말하는 현재 방식은 유지하고 시각만 다듬는다.
- [ ] **[코드] 로딩 스켈레톤.** 홈은 클라이언트 렌더링이라 첫 화면에 빈 표가 잠깐 보인다.

---

## 하지 말 것

- **애드센스를 지금 신청하지 말 것** — §2 참고. 반려 이력이 남으면 재신청이 불리하다
- **`ads.txt` 에 임시 ID 를 넣지 말 것** — 내용이 틀린 `ads.txt` 는 없는 것보다 나쁘다.
  크롤러가 이 파일을 권위 있는 목록으로 읽어 정상 광고 요청까지 거부한다. 그래서 지금은 404 다
- **보관정책 함수(`prune_timeseries`)를 지우지 말 것** — [HANDOFF §5-1](HANDOFF.md)
- **저장소를 비공개로 돌리지 말 것** — GitHub Actions 가 유료가 된다(월 ~3,000분 초과 청구).
  꼭 필요하면 외부 크론으로 먼저 옮긴 뒤에
- **결측을 0 으로 채우지 말 것** — 이 사이트가 다른 미러와 다른 유일한 지점이다.
  리뷰가 없으면 `집계 전`이지 0% 가 아니다
